# `documentation/agent-coordination/`

Phase plan and the file-ownership matrix for parallel agents (`file-ownership.md`, `implementation-guide.md`). Rule zero: an agent writes ONLY the files in its row.
