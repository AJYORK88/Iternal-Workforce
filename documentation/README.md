# `documentation/`

Project documentation. Authoritative order: `requirements.md` (locked decisions) → `documentation/design/*` (the spec) → `documentation/agent-coordination/*` (phases + file ownership). Each subfolder has its own README describing what belongs there. Unique processes or plans are saved as separate markdown files with a descriptive name, e.g. `SEO-Strategy-Plan.md`.
